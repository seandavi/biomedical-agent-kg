# Production-only env (loaded by `vite build`, not `vite dev`).
# GA4 measurement ID. Safe to commit: it's a public identifier embedded in the
# served page. `bun run dev` has no tag set, so local development never tracks.
VITE_GA_ID=G-XJ08FB8D4Z
